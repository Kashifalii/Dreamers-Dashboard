
  # Multi-role Dashboard System

  This is a code bundle for Multi-role Dashboard System. The original project is available at https://www.figma.com/design/eq6WVcGA4JyljtuxLtz8Hb/Multi-role-Dashboard-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  